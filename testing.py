

'''
Testing function

'''

class arduino:

    def __init__(self, com_address, baudrate, timeout):
        '''
        defines arduino control object with address

        Args:
            com_address(int),
            baudrate (int),
            timeout (int)

        Returns:
            Nothing
        '''


        return

    def sample(self, a):
        c = a +3
        return c



connection = arduino(4,5,6)
connection.sample(5)

